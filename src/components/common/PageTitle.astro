---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<h1>{title}</h1>

<style lang="scss">
  h1 {
    color: var(--color-main);

    @include mq-mobile {
      font-size: 32px;
    }

    @include mq-desktop {
      font-size: 64px;
    }
  }
</style>
